export default function Dashboard() {
  return (
    <div className="page">
      <section className="intro">
        <h1>Public report dashboard</h1>
        <p>
          Coming next: a live aggregation of every report, read directly from the
          Janta Voice Solana program and grouped by category and location — each
          entry linking to its transaction on Solana Explorer (devnet).
        </p>
      </section>
    </div>
  );
}
